import { type NextPage } from "next";
import { LayoutContent } from "~/components/Layout";

const Home: NextPage = () => {
  return <LayoutContent>Proyecto de Censo</LayoutContent>;
};

export default Home;
